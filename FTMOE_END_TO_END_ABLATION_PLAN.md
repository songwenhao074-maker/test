# FT-MoE 现行实验规则

当前状态只维护在 [PROJECT_CONTEXT_LATEST.md](PROJECT_CONTEXT_LATEST.md)。本文件保留规则与尚待决定的问题；本次文档维护不启动新实验。原长版计划见 [归档](docs/archive/README.md)。

## 比较关系

- 主链：v0、v1、v2、完整 v4。
- 主分支消融：完整 v4 对比门控融合替换，以及移除整套图扩展的 v2。
- v3 是简单融合补充对照，不要求优于 v2。
- 当前 v2 增加专家池及额外输入，v4 增加注意力及交互，因此不能将对应差异写成纯路由或纯注意力贡献。
- 研究单独图消息传递、匹配专家池或其他结构，需要新增对照，不能事后改名解释旧结果。

## 训练公平性

1. 各版本、各模型种子独立初始化；所有参数进入优化器，不加载前一级权重、不冻结前缀。
2. 同一对照组使用相同数据、损失、学习率、预算、优化器、调度和选模规则；公共参数初始化及 batch 顺序按种子对应。
3. 公共设置：batch=32，AdamW，weight decay=1e-4，5 轮 warmup、cosine 衰减至 1e-5，梯度裁剪 1；EAGate 温度 1 降至 0.2。
4. 损失：0.7 检测 CE + 0.3 异常资源 CE + 0.5 排名损失 + 0.01 路由平衡，检测类别权重 [0.6,2]。多个 router 的平衡项取平均。
5. 30 轮与 60 轮、不同学习率或 dropout 的结果不能按版本拼表。仅完全未改变的控制可经权重、配置和数据核验后复用，并保留来源。
6. 不以削弱基线、挑种子、挑不利 checkpoint、修改测试标签或选择最单调的评分规则制造贡献。

## 现行选模

- A_last：公共预算末轮。历史候选为第 30 轮，60 轮组为第 60 轮。
- B_best：每个模型种子分别在预算内，最大化三个开发重放等权平均的 S=(F1+HR+NDCG)/3；同分取最早轮。检测阈值固定 0.5。
- 当前单标签导致 HR=NDCG；S 并非检测与诊断等权，也不保证 B 的 F1 高于 A。
- 协议 014 预先指定 B 为主、A 为敏感性分析。原三种子、新两种子和合并五种子分别报告，不舍弃任何种子。
- 新选模规则尚未定稿。可参考 [方法调研](docs/MODEL_SELECTION_REVIEW.md)，但规则变更必须登记为新的开发分析，并对全组一致应用。

## 诊断与验收

保留每个种子/重放的数值及配对条件区间。五种子均值单调不表示每个种子均单调；已用于选模的数据上的区间也不是独立确认性证据。

协议 011/013/014 的既定开发门槛为：每项主比较 F1 增量至少 0.005、两项诊断均值增量为正；完整模型 F1/HR/NDCG 至少 0.86/0.63/0.58。协议 014 另要求新增两种子的主比较均值方向为正。这些是项目筛选规则，不是普遍的论文有效性条件。

未通过门槛的结果仍完整保留。HR 与 NDCG 在本数据上是同一诊断准确率，不作为两份独立证据。资源 macro F1 与无训练物理规则作为必要的解释性比较，避免多数类和简单规则被忽略。

## 数据与最终测试边界

当前以物理数据协议 004 为准。归一化只使用训练块，窗口不跨重放，不能将模型种子与数据重放种子混为一谈。旧数据的生命周期问题已修正，旧结果不能混入当前物理组。

新测试 201–205 保持未生成；旧测试 37/41/47 不参与调参。完成方法选择、对照和复核后才冻结全部配置并测试一次。反复使用同一开发数据的偏差，不能靠增加模型种子消除。

按原登记门槛，协议 014 未取得冻结测试资格。2026-09-05 用户另行验收当前消融阶段通过并要求归档；这一阶段决定不重写原筛选结果，也未授权启动最终测试或 QoS。旧最终评估器还需适配当前分支对照后才能使用。

## 待用户下一步指示

当前版本已由用户验收为后续实验的回档基线，见[归档说明](docs/BASELINE_ACCEPTED_20260905.md)。协议 012 配对来源门控仍未完成；选模规则、类别平衡或模型结构的新研究也尚未启动。保留每轮权重和全部失败记录，等待下一实验指示。
