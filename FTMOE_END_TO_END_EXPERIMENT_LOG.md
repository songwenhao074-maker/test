# FT-MoE 实验索引

本文件记录阶段结论和来源。当前状态见 [项目入口](PROJECT_CONTEXT_LATEST.md)，旧完整流水记录已保存在 [文档归档](docs/archive/README.md)。协议 JSON、原始日志、指标、源码快照和逐 epoch 权重不因文档精简而重写。

## 全模型实验：协议 001–014

协议文件均位于 `artifacts/ftmoe_end_to_end/`，训练目录位于其 `runs/`。

| 协议 | 实验或修正 | 状态与结论 |
|---|---|---|
| 001 | 独立全模型训练；initial_lr0003 | 15 个 60 轮完成。随后发现底层数据生命周期/输入问题，作为旧数据结果保留；initial_lr001 部分运行停止 |
| 002 | 公共停止轮次选择 | 原顺序链登记标准未通过；选择记录保留 |
| 003 | 修复容器生命周期与 trace 绑定 | 修复并重建重放，需求对齐检查通过 |
| 004 | 真实动作前主机输入、实际过载资源标签 | 当前物理数据基准；physical_lr0003_e30 全组完成 |
| 005 | 修正资源诊断指标与重选检查点 | HR@100%、NDCG@100% 改为逐异常样本诊断；旧每轮混淆矩阵重评，原全局 top-100 只保留作补充 |
| 006 | 公共学习率 0.0003/0.001/0.003、30 轮 | 三配置共 45 个模型完成；按五版本平均 B 综合分数选择 0.003，非按链形状选择 |
| 007 | lr=0.003 扩至公共 60 轮 | 15 个模型完成，前段诊断及后段检测仍有回落 |
| 008 | 公共阈值网格校准 | dropout=0.1 的 attention 与 gated 60 轮组已校准，未通过；不改变原最佳 epoch |
| 009 | 普通 MoE 的 none/local/global 上下文 | 单模型种子 pilot 完成；原 none 的平均综合分数最高，新增上下文未采用 |
| 010 | 专家 dropout=0/0.1/0.3 | 单种子 pilot 选择 0.1；v1–v4 三种子 60 轮完成，未改变的 v0 按来源哈希复用 |
| 011 | 用户修订主链及分支消融 | v0/v1/v2/v4 为主链，v3 为补充。同参数门控三种子完成；原始与校准结果均未达门槛 |
| 012 | 同主机内部的时间/图来源融合 | source_attention 三种子 60 轮完成；source_gated 尚未训练，配对实验未结束 |
| 013 | 回顾旧 30 轮低学习率候选 | 原始源码/更新逻辑/检查点核验通过；补齐三个同配置门控模型，均值方向为正，融合检测增量不足 0.005 |
| 014 | 固定旧配置，新增模型种子 17/42 | 五版本及门控共 12 个模型完成；五种子主比较均值方向为正，但小效应门槛和新种子确认未通过；测试保持封闭 |

协议具体文件名可由 `protocol_*.json` 查阅。编号表示登记顺序，不表示每个实验已完成或成功。

## 最新复用与确认产物

- [历史审计](FTMOE_HISTORY_REUSE_AUDIT.md)：重新比较全部可比完整组，保留 A/B、失败配置和单种子筛选的边界。
- [五种子确认](FTMOE_HISTORICAL_CONFIRMATION.md)：原三种子、新两种子、合并五种子分别报告。
- [选模诊断](FTMOE_HISTORICAL_SELECTION_DIAGNOSTIC.md)：部分早期基线的高诊断准确率来自近乎恒定的 CPU 预测。
- `history_runtime_compatibility.json`：旧源码、数据 manifest、更新逻辑和五版本初值/单步更新一致性。
- `history_reuse_audit.json`、`comparison_014_complete.json`：来源哈希、指标、配对差值及条件区间。
- `diagnosis_reassessment/physical_lr0003_e30/`：旧组 B 必须使用这里指定的每轮权重，不能直接用原 best.pt。
- `comparison_011_raw.json`、`comparison_011_calibrated.json`：60 轮原 attention/gated 对照结果。
- `figures/historical_confirmation_014.png`、`.svg`：五种子 A/B 对比图。

历史候选新增三个门控模型及协议 014 的十二个模型，均统一 30 轮、lr=0.0003、dropout=0、独立全参数训练。没有因结果不利而丢弃种子。完整 v4 的五种子 B F1=0.905134，对 v2/gated 的增量为 0.003799/0.001554；新种子中门控检测均略优于 attention。

## 保留的其他证据

- `physical_rule_diagnostic.json`：无训练物理规则开发 F1≈0.93057、诊断≈0.98194。
- attention 推理扰动：同主机替换有小幅改善、去除交互有明显损失，但推理扰动不能代替独立重训消融。
- 上下文和 dropout pilot 只用于筛选，不是完整多种子确认。
- 匹配单专家池路由、容量比例图编码候选已有代码/单元检查，尚未接入正式训练。
- 同一 workload 的调度重放、诊断多数类和重复开发集选模仍是已知限制。论文指标不能直接横向宣称优劣。

## 更早的冻结训练

[FTMOE_ABLATION_FINAL_REPORT.md](FTMOE_ABLATION_FINAL_REPORT.md) 原文保留。它对应不同预算、逐级冻结训练和旧指标的历史研究，不能作为当前独立全模型实验已成功的证明。

旧 v8/v9 笔记、早期执行计划和旧上下文移入归档；它们的运行代码、唯一权重及数据不在此次删除范围。2026-09-05 维护的文件级清单与哈希验证见 `maintenance/cleanup_20260905/`。

## 最近讨论：选模方法

用户质疑仅按综合平均分选 epoch 的充分性。已搜索官方实现与论文，结论与引用见 [选模方法说明](docs/MODEL_SELECTION_REVIEW.md)。平均分有先例，但当前 HR=NDCG 造成诊断重复计权；尚未改用 F1 优先、约束或 Pareto 规则，也未重写既有实验结论。

## 2026-09-05 用户验收与版本归档

用户确认当前消融阶段可以认为通过，要求保存当前版本供后续实验回档。以协议 014 五种子结果及当前代码、数据、全部现存权重为基线建立独立快照；具体范围、SHA-256 校验与恢复命令见[归档说明](docs/BASELINE_ACCEPTED_20260905.md)。这是用户阶段验收决定，不修改协议 001–014、比较 JSON、历史报告或既定筛选判定。未变更模型、训练参数、指标或选模规则，未启动下一实验、最终测试或 QoS。
