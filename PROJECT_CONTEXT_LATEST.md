# 项目最新上下文

本文件是唯一当前状态入口。历史原文见 [文档归档](docs/archive/README.md)，详细数值见 [确认报告](FTMOE_HISTORICAL_CONFIRMATION.md)。

## 当前状态与用户要求

- 用户于 2026-09-05 确认消融阶段通过并已完成回档归档；随后批准在线微调实验计划，并要求由当前助手独立执行，不创建子智能体。
- 独立初始化、全参数训练的实验已执行至协议 014；没有训练进程运行。
- 最新完整候选：lr=0.0003、30 轮、expert dropout=0、模型种子 1/2/6/17/42。
- 当前阶段状态：用户验收通过，作为后续实验的回档基线。五种子均值方向支持主链；原协议的小效应门槛及新增种子确认未通过，原始数值、筛选判定和局限保持不变。用户阶段验收不等同于新增统计证据。
- 协议 012 的 source_attention 三种子已完成，source_gated 尚未训练，配对实验仍待补齐。
- 新测试重放 201–205 从未生成或评估。旧测试 37/41/47 不参与当前开发。暂不运行 QoS。

## 在线微调实验：协议 015

- [执行说明](docs/FTMOE_ONLINE_EXPERIMENT.md)：相同 v4 B_best 起点，A不微调、B全参数、C固定MoE、D动态EAGate；原生 BWGD2/GOBI 统一轨迹。目标为五模型种子×三回放种子的后半程平均 F1≥0.65 且 D>B。
- 已新增独立在线包装、原生采集器、延迟标签顺序更新器、分析绘图及分阶段入口。旧模型与模拟器源码未修改。
- 核心9项与报告3项测试通过，共12项，覆盖旧权重输出一致、冻结、因果性、增删优化器状态、恢复一致性、空F1及配对完整性。
- 首次采集因内存保护停止，失败目录已保存在 `artifacts/ftmoe_online/failed_attempts/seed301_steps300_attempt001/`。用户要求再次开始后，保持原配置完成模型种子1、回放种子301的300步四组小试。
- 后半程主F1：A=0.412500、B=0.416667、C=0.419067、D=0.408207。D低于0.65且低于B，按约定停止；参数搜索与五种子确认未启动。正常4,245、异常555满足样本门槛，但异常全部为CPU。D第100步从4专家增至5，没有观察到收益。
- 详情见[小试结果与问题报告](docs/FTMOE_ONLINE_PILOT_015.md)；机器状态见 `artifacts/ftmoe_online/status.json`。固定离线参考集F1仍约0.905，建议先审计离线与原生输入、标签及分布差异。没有降低资源门槛、修改模拟器或注入漂移；后续实验待讨论。
- 后续[分布审计及待审批方案](docs/FTMOE_ONLINE_DISTRIBUTION_AUDIT_015.md)已完成：离线OfflineTraceWorkloadV2与原生BWGD2在需求、容量、I/O语义及任务机制上显著不同；第14主机存在退化归一化，单独兜底后冻结组后半程F1仅从0.4125到0.4224。标签与实际迁移一致性检查通过。原生磁盘需求1–3MB在当前容量下不可能产生磁盘空间异常；图输入尚未区分槽位更换容器。推荐共同输入修正和原生同域共同离线适配，尚未实施，等待用户评审。
- 用户进一步要求评估通过模拟器对齐旧模型分布。[保留原014模型的模拟器对齐方案](docs/FTMOE_ONLINE_MATCHED_SIMULATOR_PROPOSAL.md)已完成，尚未实施：恢复旧OfflineTraceWorkloadV2映射、任务机制和容量，先复现原202步作为工程检查，再评估连续长流。新增发现version1/version2各有10002步候选数组，并非原202行简单循环，但来源记录不完整，分布仍不同；正式使用须先核验来源。当前评审优先讨论此路线，不自动启动共同再训练。
- 用户明确只需异常分布大致相似，并希望保留BWGD2处理输入。[BWGD2需求适配方案](docs/FTMOE_BWGD2_INPUT_ADAPTER_PROPOSAL.md)提出CPU限幅、RAM×2、I/O训练语义、恢复容量及合成动态磁盘。随后用户批准单次尝试，实施结果见下方协议016；静态需求量级接近不代表实际异常分布达标。

## 最新小试：协议016，BWGD2需求适配

- 用户批准尝试一次，已新增独立需求适配器及训练统计驱动的合成动态磁盘，完成种子301的300步加确认间隔。原模拟器核心与015入口未改，3项专项测试通过。
- 主异常比例2.90%，低于登记5%–25%；主标签CPU/RAM/磁盘106/3/30。按约定停止在线更新，仅运行冻结A诊断，B/C/D未运行。
- 冻结A全程F1=0.213813、后半程=0.143911。后半程召回率79.59%、误报率19.31%，均好于原015冻结A对应值，但异常占比更低，F1不能直接解释为模型恶化或遗忘。
- 训练CPU近空闲比例38.52%，适配流仅1.71%；三容器聚集主机比例约10%对约1%；RAM上尾不足。第14主机归一化仍病态，内存中兜底后后半程F1仅0.168467。
- [完整结果与措施](docs/FTMOE_ADAPTED_BWGD2_PILOT_016.md)。推荐下一步共同归一化兜底及CPU状态/RAM尾部的无模型分数校准，尚未实施；不自动扩大实验。当前状态位于 `artifacts/ftmoe_online/adapted_bwgd2_016/status.json`。

近期方案评审：[替代数据集、容量调整与异常生成方法](docs/FTMOE_DATASET_AND_CAPACITY_REVIEW.md)。已核验Google2011长期CPU/RAM/本地磁盘空间字段、Alibaba2017短流空间字段、Alibaba2018及RCAEval的磁盘I/O语义。对016旧轨迹仅做静态容量敏感性分析：RAM×0.5主异常5.23%，磁盘×0.75为6.98%，但类别构成偏移；这些不是模拟器重跑结果。机器结果在 `artifacts/ftmoe_online/scenario_review_20260905/`。推荐预登记单因素容量对照、后续Google2011小样本审计，均尚未实施。

## 正在执行：容量017与Google2011协议018

用户已授权先降低RAM/磁盘做完整回放，随后Google2011实验。[执行登记](docs/FTMOE_CAPACITY_GOOGLE_EXECUTION_017_018.md)：017固定九组300步开发候选，仅按训练标签分布选容量，再以种子302运行2000步四组；018拟下载官方连续前缀，64步尺度校准后做300步四组小试。旧归一化和在线规则保持，分布匹配失败仍按本轮要求报告完整诊断。3项新测试通过。容量实验已启动；Google入口已实现，数据尚未完成下载。完成状态分别查看 `artifacts/ftmoe_online/capacity_017/result.json` 和 `google2011_018/result.json`，不存在时不可称已完成。

## 最新结果：协议 014（归档）

B_best 为每个模型种子在公共 30 轮预算内，按三个开发重放的平均综合分数选出的 checkpoint；不是 F1 最高轮，也不是跨五种子统一选某一轮。五种子汇总如下：

| 模型 | F1 | HR=NDCG |
|---|---:|---:|
| v0 | 0.525479 | 0.860048 |
| v1 | 0.567550 | 0.860922 |
| v2 | 0.901334 | 0.936297 |
| v3 简单融合 | 0.894437 | 0.962851 |
| v4 完整注意力 | 0.905134 | 0.972003 |
| v4 门控替换 | 0.903579 | 0.970907 |

HR、NDCG 的五种子均值在 A_last 和 B_best 下均满足 v0<v1<v2<v3<v4，完整 v4 也高于门控。F1 的研究主链为 v0<v1<v2<v4，不要求 v3>v2。

v4 相比 v2、门控的 B F1 增量为 0.003799、0.001554，均小于此前登记的 0.005，条件区间跨零。新增种子 17/42 中门控检测均略高于完整注意力，v1 诊断也没有稳定改善。0.005 是本项目开发筛选门槛，不是论文消融有效性的普遍要求；失败结果仍可如实报告。

原三种子历史组 B F1=0.546070/0.596316/0.898028/0.893558/0.903615，已核验可复用，但不能只保留这组而省略新增种子。

## 模型及贡献边界

实现入口：`recovery/PreGANSrc/src/ftmoe_ablation.py`，全模型包装：`ftmoe_end_to_end.py`，训练器：`train_ftmoe_end_to_end.py`。

| 版本 | 实际增加的计算 |
|---|---|
| v0 | 逐主机两层 Transformer、公共检测/分类头 |
| v1 | 普通 softmax MoE，4 专家 |
| v2 | 第二个 4 专家 EAGate 池，以及拟调度后的资源聚合输入 |
| v3 | 调度感知图编码；图特征残差与图预测 logits 相加 |
| v4 | 保留 v3，增加跨主机多头交叉注意力、同主机逐元素交互、归一化与输出 adapters |

v2-v1 不是纯路由替换；v4-v3 不是纯注意力消融。移除图分支及依赖融合对应 v2，只能检验整套图扩展。门控对照保留交互等其他计算，以同参数量 16,640 的同主机门控替换注意力。

## 数据、选模与已知局限

- 当前数据：`artifacts/ftmoe_end_to_end/data/protocol_004_physical`；训练重放 42/1/6/17/23，开发重放 31/101/102。模型种子与重放种子属于不同层级。
- 16 主机、7 特征、12 步窗口；每个完整重放 202 步，不跨块构造窗口；归一化仅使用训练数据。
- 已修复生命周期/trace 绑定；输入为真实动作前主机信息，标签为实际动作后过载及主导资源类别，块内 ±1 步容差。
- HR@100%、NDCG@100% 在当前单资源标签下均等于真实异常样本上的资源 top-1 准确率，包含检测漏报；不能混用旧全局 top-100 检索指标。
- 当前 S=(F1+HR+NDCG)/3，实际给诊断两倍权重。早期基线可能几乎只预测 CPU；始终预测 CPU 的开发诊断准确率约 0.898224。
- 单一底层 workload、多次开发集选模及类别不均衡均限制结论。无训练物理规则的开发 F1≈0.93057、诊断≈0.98194，高于当前候选。
- 论文参考 F1/HR/NDCG=0.8766/0.6496/0.6021；数据、标签及指标实现有差异，不能据数值更高声称复现成功或优于论文。
- 用户已要求搜索选模依据；调研见 [选模方法说明](docs/MODEL_SELECTION_REVIEW.md)。尚未批准或实施新的主选模规则，旧结果不重写。

## 继续工作入口

0. [已验收版本与回档说明](docs/BASELINE_ACCEPTED_20260905.md)：完整快照、校验清单与恢复方法。后续实验不得覆盖此归档；启动下一实验须等待用户指示。
1. [现行实验规则](FTMOE_END_TO_END_ABLATION_PLAN.md)：比较关系、训练公平性、选模与测试边界。
2. [实验索引](FTMOE_END_TO_END_EXPERIMENT_LOG.md)：协议 001–014、失败原因与产物入口。
3. [历史复用审计](FTMOE_HISTORY_REUSE_AUDIT.md)、[五种子确认](FTMOE_HISTORICAL_CONFIRMATION.md)、[选模诊断](FTMOE_HISTORICAL_SELECTION_DIAGNOSTIC.md)。
4. 机器结果：`artifacts/ftmoe_end_to_end/comparison_014_complete.json`；图：`artifacts/ftmoe_end_to_end/figures/historical_confirmation_014.png`。
5. 旧冻结增量训练：[历史最终报告](FTMOE_ABLATION_FINAL_REPORT.md)。该报告不是当前全模型实验的成功证明；其原文、权重与测试结果保持原样。

协议 012 评估器 `evaluate_ftmoe_source_alignment.py` 需等待两个来源融合臂的训练及公共阈值校准均完成后使用。旧 `evaluate_ftmoe_end_to_end.py` 仍按五节点顺序链验收，不适用于最新分支消融，不可直接启动最终测试。

历史低学习率组 B 权重必须读取 `diagnosis_reassessment/physical_lr0003_e30` 指定的每轮 checkpoint，不能拿原 best.pt 代替。全部逐 epoch 权重保留，允许后续统一重评选模规则。

运行 Python：`D:\Anaconda\envs\dynmoe\python.exe`。同一时刻只运行一个训练/重放/QoS 进程，CPU 3 线程、interop 1、DataLoader 0、BelowNormal；RAM 至少 4.5 GiB、磁盘至少 20 GiB。维护记录见 [文档索引](docs/README.md)。
