# 在线微调小试：重试完成，性能未达标

## 当前结论（2026-09-05）

用户要求“再次开始”后，保持协议015原配置重新执行。原生轨迹采集及A/B/C/D四组300步小试均已完成。D后半程F1为0.408207，低于0.65且低于B的0.416667；流程按约定停在小试阶段，未进入学习率搜索或五种子正式确认。当前机器状态为 `awaiting_user_discussion`。

本次仅有模型种子1、回放种子301这一个配对，不能作为五种子确认结果。报告中对此单配对重采样得到的区间会退化为一个点，不具有独立重复下的置信区间证据意义。

## 完成的小试结果

四组使用同一个协议014指定的 `v4_seed1/checkpoints_by_epoch/epoch021.pt`，B/C/D学习率均为3e-5。F1由对应时间段全部主机预测合并计算，主标签采用延迟确认的±1步容差。

| 组别 | 全程F1 | 前半程F1 | 后半程F1 | 后半程原始物理标签F1 |
|---|---:|---:|---:|---:|
| A：不微调 | 0.456718 | 0.502709 | 0.412500 | 0.284010 |
| B：普通全参数微调 | 0.449339 | 0.482690 | 0.416667 | 0.287912 |
| C：固定专家MoE微调 | 0.458650 | 0.500000 | 0.419067 | 0.291525 |
| D：动态专家MoE微调 | 0.456009 | 0.503198 | 0.408207 | 0.286070 |

D−B为−0.008459，D−C为−0.010859。四组后半程均低于前半程，但曲线存在回升，不能称为持续单调退化，也不能支持“普通微调退化而D保持高性能”的预期。

300评分间隔共4,800个主机样本。主标签正常4,245、异常555；原始物理标签正常4,494、异常306，满足正常和异常各至少50的样本门槛。异常全部属于CPU，RAM和磁盘类别支持数均为0；本流不能证明三类资源诊断的泛化能力。

D在第100步新增专家，专家数由4变为5；第200、300步均未增删。首个调整周期1,600个输入中有118个在保底选择前未匹配，后两个周期未匹配数均为0。机制实际触发，但本次没有观察到动态增删的检测收益，不能据此单次运行断言新增专家导致性能下降。

固定离线开发参考集初始F1为0.905066，结束时A/B/C/D分别为0.905066/0.902682/0.905922/0.904454。旧参考集性能基本保持，而冻结组在原生流上也明显偏低，提示应优先排查离线与原生输入、标签及数据分布的差异；这仍是待验证的解释，尚不能定位根因或把低分全部归因于遗忘。

采集301间隔（含末尾标签确认间隔）耗时86.43秒；四组运行器记录耗时分别为2.08/6.47/4.35/4.50秒。上述分项不包括全部进程启动、报告生成等开销。后续长流和动态拓扑可能改变开销，当前未开展正式确认。

结果可独立重算，产物如下：

- [指标与阶段判断](../artifacts/ftmoe_online/pilot/analysis/report.json)、[对比表](../artifacts/ftmoe_online/pilot/analysis/comparison.csv)。
- [曲线图PNG](../artifacts/ftmoe_online/pilot/analysis/online_comparison.png)、[SVG](../artifacts/ftmoe_online/pilot/analysis/online_comparison.svg)。
- [轨迹清单与哈希](../artifacts/ftmoe_online/streams/seed301_steps300/manifest.json)；逐步预测、更新、专家事件及恢复状态位于 `artifacts/ftmoe_online/pilot/runs/` 下四个独立目录。

共同轨迹SHA256：`42c163fb4cd520a91a7bca30d22cdc6bb29438cb388fe782806819ca9132c65c`。

## 实现与验证

已实现获批的四组同源 v4 在线实验：稳定专家编号、动态增删、存活优化器状态保留、容量适配、冻结范围、延迟容差标签、顺序预测、64间隔缓冲区、断点恢复、独立指标重算、绘图、学习率选择和五种子配对分析。

`test_ftmoe_online.py` 的9项核心验收测试通过（8.476秒）。真实离线权重转换后 logits 逐项完全一致；未来标签变化不影响此前预测和更新；动态拓扑事件后保存、恢复与连续运行的最终参数及预测完全一致。测试中的人工标签仅用于单元测试，不是实验数据。

`test_ftmoe_online_reporting.py` 的3项报告测试通过（4.800秒），包括无异常样本的空F1与绘图、完整配对验收、不同起始状态的拒绝。累计12项通过；记录与源码哈希见 `artifacts/ftmoe_online/implementation_verification.json`。这些测试不代表真实模拟器小试已完成。

## 历史：首次采集因内存保护停止

2026-09-05 首次执行 `run_ftmoe_online_stages.py --through confirmation`，从回放种子301、300评分步的小试开始。采集器成功加载原生 BWGD2 数据与GOBI预训练模型，随后在主循环触发：

```text
RuntimeError: RAM guard below 4.5 GiB
```

首次尝试按已批准的停止规则退出。当时没有完整 `stream.npz`/manifest，也没有启动A/B/C/D四组在线训练，因此该失败尝试没有可报告的在线F1对比。随后重试的结果见本报告开头。

原失败目录在重试前完整移至 `artifacts/ftmoe_online/failed_attempts/seed301_steps300_attempt001/`，其中保留 `failure.json`、`generation.log`，另存首次流程状态 `pipeline_status.json`。失败当刻的具体可用内存、完成步数未被首版日志记录，不作推断；已补充后续故障的内存数值和完成步数诊断。此诊断改动不改变模拟器行为或门槛。

子进程退出后的只读检查显示：系统总物理内存约15.19 GiB，可用约4.93 GiB。该值是退出后的快照，不能代表运行时余量。未终止其他应用或降低4.5 GiB门槛。

## 后续边界

本次重试启动前可用内存约6.32 GiB、磁盘余量约113.82 GiB，资源保护门槛保持不变；没有终止其他应用。原回档包、旧模型、原模拟器、协议001–014与预留测试均未改动。

真实采集、样本检查及四组小试已完成，但性能前置条件不满足。建议下一步先审计离线与原生输入和标签的差异，再讨论后续实验。开发学习率搜索与正式确认尚未启动；不自动修改模拟器、增加压力、注入漂移或更改专家规则来制造预期走势。
