# Protocol-030分析（2026-09-22）

证据基点：main 09560d348a94af356e1d6b7e5fe0f08fcbf5aba9；[030结果](PROTOCOL030_RESULTS.md)、[配对检查](../artifacts/ftmoe_online/protocol_030/runs/run_35716480519/paired_consistency.json)、[机会审计](../artifacts/ftmoe_online/protocol_030/runs/run_35716480519/opportunity_audit.json)。运行35716480519，完整artifact10690558198。

## 结论
030完成了诊断目标，可以结束本轮复现排障，进入真实在线策略试验。同机单线程环境中，audit_off/on检测与分类概率最大差均为0、逐元素一致比例100%；九窗及全程AP相同，981个离散事件及最终拓扑一致，旁路状态违规0。此证据支持已登记环境中的观察器无干扰，不追溯更改029失败结论。

历史028分类概率最大差仍为1.4305114746e-6，所以历史逐元素门禁未通过。它与新同机配对有效性是两个不同问题，不能据此再要求完整重跑030。恢复报告排除了混入计时字段的原始controller摘要；其余明确列出的模型/优化器/RNG/拓扑/模式/缓存字段一致。不要扩大表述为所有控制器字节均已逐项核对。

Action显示failure来自两次全流回放完成后的numpy.int64 JSON序列化，不是训练/策略失败。完整artifact离线恢复没有消耗第三次回放。另一个工程尾项：ARTIFACT_INDEX里status.json仍列旧251字节，而恢复后Git文件为397字节；应区分原始artifact文件索引和恢复报告索引，不能据此声称原始artifact损坏。031必须先验证结果序列化与最终清单生成。

## 已确认的机制障碍
154个reuse到期点中，60个有休眠记忆、59个完成至少一个候选验证、1个流尾截尾。5条满足损失及normal/FPR/guard验收条件的记录，全是专家9，位于S6_first的3937/3969/4001/4033/4065：
- 五次都被新生候选训练/验收的busy状态阻挡。
- 五次专家9均已排名第一，但相似度约0.757–0.951，低于约0.988的硬准入阈值。
- 因此不是“排序没选到正确专家”；增加top-k不直接解决已发现的问题。只改busy或只取消相似度硬门槛也不能同时解除两道限制。
- 五次通过的是后续预测验收条件，不包含上述匹配/调度条件；它们不是五次实际复用，更不是五个独立证据。一次真正切换会改变后续训练、候选和收益，不能要求新策略复现相同五次事件。

业务含义：新业务适配正在后台训练，不应必然阻止尝试已有专家；输入形态相似度可用于排队，而短期标签前预测及成熟标签可用于决定是否部署。这是可部署的机制假设，不需要业务ID、指定专家9或知道未来阶段。

## 对D>C目标的限制
这些机会全部发生在S6首次出现，而非九个回归主窗口。030紧凑恢复报告未附完整逐专家九窗口AP/FPR表，不能把029的未通过门禁表格直接包装为030已验证结果。当前可确认的是存在被阻挡的局部复用机会，尚未证明它足以改善回归主指标。

可靠D/C参照仍为028：C/D九窗均值0.769551/0.766681，D-C=-0.00286984，正差3/9；C/D全程AP=0.719730/0.711047。030没有重跑C，不新增排名或成本结论。C是固定残差专家，不是全量微调；D尚无总成本优势。

## 唯一下一步
[Protocol-031：非阻塞、经未来标签验收的复用策略D/C试跑](PROTOCOL031_SINGLE_TASK_DIRECTIVE_20260922.md)。

只检验一个策略包：相似度用于挑选一位候选但不做硬拒绝；该候选的16步因果验证可以与新生训练并行，真正接管仍须满足原全部预测/正常背景验收条件。这是联合策略试验，不能把结果单独归因于取消门槛或解除busy。

C_fixed5和D_nonblocking_reuse各一次，同冻结流、seed700/model1、同期同执行环境。九窗口主指标保持不变；复用成功与预测胜出分开判定，S6_first收益单列为次要结果。两次回放完成即停止，不自动切换场景、追加A/B或调参。
