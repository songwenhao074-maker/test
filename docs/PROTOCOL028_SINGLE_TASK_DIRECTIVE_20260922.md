> 历史指令，已完成：run35705211072；以下为当时登记原文，含当时planned状态，不代表当前任务。见[028结果](PROTOCOL028_RESULTS.md)；当前唯一任务为[029旁路诊断](PROTOCOL029_SINGLE_TASK_DIRECTIVE_20260922.md)。

# Protocol-028：保护已验收记忆的一次D/C开发试跑
日期：2026-09-22。状态：planned_not_implemented_not_run。基点：81756266234e7649de76b92a74a22b82c2bbbaee及其已完成Protocol-027结果。

## 唯一任务
在既有六业务更替流上，检验“容量满时不为尚未验收的新候选预先删除已验收记忆”能否恢复有效复用，并改善D相对C的回归表现。实现这一项策略差异，只运行C_fixed5与D_memory_protected各一次，报告结果后停止。

## 唯一科学改动
继承Protocol027NormalGuardLifecycle；仅修改满容量新生准入：
- 驻留数小于8：沿用027的新生、验收、冻结和替换流程。
- 驻留数达到8且需要shadow：跳过此次birth，保留全部已验收记忆，不执行现有的purge最老休眠专家。不在磁盘偷偷存一份后继续计为已删除，不提高容量。
- 当前已到期的reuse仍按原逻辑执行；监控阶段同点reuse优先规则不变。记录birth_skipped_capacity_preserve_memory及专家ID，计数守恒。
- 不用业务ID、阶段边界或未来标签决定跳过；决定只依赖当前容量与状态。真实代价是缓存满后可能无法学习新的业务，必须如实讨论。

不改变数据、seed700/model1、初始化、在线优化、64步replay、候选128/32、reuse每32步/验收16步、相似度规则、1%改善、正常guard、活动/渐变专家限制、最多8驻留专家或九窗口指标。不得为了触发reactivation降低验收门槛。旧027实现和记录保留，028用独立入口/注册/输出目录，不能覆盖旧结果。

## 执行与复现
使用完整冻结artifact：run35682811782，artifact10675401651，名称protocol027-frozen-data-35682811782；ZIP digest为111a4c5fc5c508a9e169fdbffe823ee36dc66c037c1775a6aa1abdbaba31c0a1。沿用protocol027_data_revision_002的stream、事件表与全部冻结文件，保留其027数据注册快照；028方法改动另登记，不能改源快照导致旧冻结包失效。不重新模拟。

两组在同一次执行环境中独立进程、顺序从头运行。C配置不变，也运行一次以形成同期对照；旧D结果仅作历史参照，不自动再加第三组。修复数据交接的代码不得夹带其他算法改变。

执行模型先实现028入口和工作流。连接器无workflow_dispatch时，在新分支protocol-028-memory-protected-20260922配置新文件.github/workflows/protocol028-pilot.yml的push触发；普通结果提交不触发。不要仅在旧027工作流加启动注释，否则运行的仍是旧方法。实现完成后用一次明确的启动提交触发，不自动追加种子或再调参。

最小必要检查：满容量时休眠专家和参数不变、不创建shadow、跳过计数增加且守恒；未满容量行为与027一致；复用激活不增加驻留数超过8；两组绑定同一冻结数据。检查和此次两组回放构成同一个任务，不扩成独立测试项目。

## 固定判断与交付
仍报告全部九回归first100窗口的等权AP差D-C、正差窗口数、同窗口合并FPR差及全程指标；继续沿用9/9有效、平均差≥0.03、至少6/9正差、FPR差≤0.01的开发参考。数值差>0只能称本轨迹数值领先，不能称统计确认。主指标不变，不仅挑最后S3窗口报告。

同时回答：原先会被预删的专家是否保留；六个后期回归窗口中实际发生了哪些匹配/验收/复用；reactivation是否>0；每次reuse接受或拒绝的损失改善、normal/FPR/guard数值与原因。增加复用次数不等于提高AP，未发生purge不等于证明删除机制有效。

记录两组墙钟、CPU、RSS、预测p95及候选/复用开销，并准确区分计时包含的循环内诊断和未包含的最终保存；不改变算法来做顺带性能优化。输出独立028 comparison/status、生命周期和逐次reuse决策紧凑JSON、完整artifact链接与SHA，更新结果页与LATEST到main。解释一个最主要限制，交付后停止；不得自动追加A/B、其他固定拓扑、消融、确认种子或参数搜索。
