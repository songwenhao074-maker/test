# 文档与历史文件维护

用户要求先完成项目整理，再等待下一个任务。本轮没有启动实验、修改模型或选模规则、打开测试集或运行 QoS。

## 文档整理

- 将 PROJECT_CONTEXT_LATEST.md 收敛为唯一当前入口，移除旧“实验已完成、立即做 QoS”等过期引导。
- 将全模型长计划整理成现行规则，将完整流水记录整理成协议索引。
- 保留三个可复用的结果报告及冻结历史报告原文。
- 新增选模方法调研、文档索引和归档说明；README 指向当前状态，区分模拟器演示与离线全模型实验。
- 清理前的 22 个文件已打包并逐文件核验，可从 ZIP 查阅历史原文。

## 文件清理边界

清理计划列出 454 个文件，共 270,338,984 字节（约 257.82 MiB）：112 个已完成训练的 resume.pt、324 个可重建 Python 缓存、18 个已归档的旧根目录文件。归档包约 0.39 MiB；删除字节数不是最终目录净缩减值，后者还包括新增文档和审计清单。

每个删除的 resume.pt 都核对了完整训练摘要、末轮编号、日志、best/last 和全部逐 epoch 权重，并逐 tensor 验证其模型与 last.pt 相同。删除的部分是完成任务后的 optimizer/RNG 恢复状态；相应完整训练不能再从原恢复文件直接恢复优化器状态，但已有模型推理与逐轮选模分析不受影响。

未完成运行，以及未满足逐轮产物完整检查的 16 个恢复文件保留。所有逐 epoch checkpoint、best/last、数据、源代码、配置、指标、失败记录、源码快照和唯一旧备份继续保留。没有按实验效果好坏删运行。

旧大 checkpoint 经核验不是逐字节重复，因此没有为释放空间而删除。根目录 Python/脚本路径也保持不变，避免破坏 import 和源码哈希。

## 可核验记录

清理后验证已完成：454 个计划文件已删除，22 个归档原文的字节哈希匹配，7,361 个保留文件均与维护前一致。根目录 Markdown 从 17 份、148,562 字节减少至 8 份、37,399 字节，体积减少约 75%。12 份活动文档中的 41 个本地链接全部有效，没有断链。

- [清理计划与归档清单](../maintenance/cleanup_20260905/plan.json)：每个目标的原因、大小、删除前 SHA-256。
- [删除回执](../maintenance/cleanup_20260905/deletion_receipt.json)：实际处理的文件和空目录。
- [保留文件哈希](../maintenance/cleanup_20260905/protected_sha256.json)：7,361 个代码、数据、权重、配置及指标文件。
- [最终核验](../maintenance/cleanup_20260905/verification.json)：删除、归档和保留内容校验及文档精简统计。

删除由同一 PowerShell 脚本执行：先核验工作区绝对路径、拒绝符号链接/重解析点及应用元数据，再核验全部待删文件与归档哈希；仅删除明确文件及确认空目录，没有递归删除。

维护脚本是一次性审计工具，不应未经重新清点直接重复执行。当前等待用户下一步指示。
