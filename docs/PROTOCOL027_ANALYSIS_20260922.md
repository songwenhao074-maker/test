# Protocol-027结果分析（2026-09-22）
来源：[run35682811782](https://github.com/songwenhao074-maker/test/actions/runs/35682811782)，结果提交81756266234e7649de76b92a74a22b82c2bbbaee。两组完成、资格审计通过、完整数据已归档；不是此前的数据恢复失败。没有启动新实验。

| 指标 | C_fixed5 | D_dynamic |
|---|---:|---:|
| 全程检测AP | 0.719730 | 0.711054 |
| 九回归窗口前100步等权AP | 0.769551 | 0.766751 |
| 全程Recall（阈值0.5） | 0.659607 | 0.644186 |
| 全程FPR（阈值0.5） | 0.047329 | 0.046978 |
| 模型循环墙钟秒 | 47.375 | 478.422 |
| 模型循环CPU秒 | 94.738 | 632.059 |

主指标D-C=-0.00279999，3/9窗口为正，development_signal=false；全程差=-0.00867628。D墙钟约10.10倍、CPU约6.67倍。本轮不能支持“D更准”或“D总计算更省”。这仍是单个开发轨迹，不能将小分差称为统计显著或推断对其他情形必败。

## 已有证据指向的问题

1. 增加和退出发生了，复用没有发生：20个新生候选，4个接受、15个拒绝、1个尾部未决；4次birth、3次retirement、1次purge、0次reactivation。154次到期复用机会中101次busy（65.58%），32次无休眠记忆，16次不匹配，只有5次进入验收且全部拒绝。计数守恒，不是日志漏算。5次拒绝的逐项原因没有保存在本次成功读取的紧凑汇总中；完整artifact下载返回403，故不将birth拒绝原因冒充reuse拒绝原因。
2. 形成记忆过晚且覆盖不足。首个通过专家9在cursor2043接受，2044开始影响预测，已到S3首次阶段尾部；S1、S2首次阶段都没有通过的新专家。其余接受ID为15（3579）、17（4091）、18（4347）。6个业务阶段并未自动产生6份可复用记忆；不能把时间线上“有回归”当成方法一定“有记忆”。
3. 满容量准入存在可检验的结构问题。源码maintenance/protocol024_v2b_parts/part02.pyfrag的_free_capacity_for_shadow在新候选启动之前就purge最早验收的休眠专家。已提交记录显示只删除1个专家，最终记忆为15、17、18，故被删的是9；结合容量、接受时刻和代码顺序，可定位到cursor4441创建候选19时。19随后被拒，9却不会恢复。9形成于S3，而S3_rec2从5340开始。这里“未验收候选先挤掉已验收记忆”的机制确定存在，但删除9对最终AP的因果影响仍须实验验证。
4. 忙碌占用与短回归不匹配。新生候选128步训练、32步验收，加标签成熟约162步；单次回归180步，主指标又只看前100步。新建优先占满多数时间，不能代替快速复用。现有代码在monitoring且同一时点到期时已经先检查reuse，问题不能简单描述成“代码从不优先reuse”。
5. 报表需区分成本范围。D的shadow训练单独约32.45秒，无法据此解释全部额外墙钟。实际计时围绕step循环和finish，最后save在计时之后；循环内生命周期诊断没有被扣除。因此现有diagnostic_and_file_io_included_in_wall_and_cpu=false过于笼统，后续应准确写明范围，不能把总慢速全部归因于shadow训练或纯推理。

另有交接问题：最新结果原先只在protocol-027-pilot-gpt56-20260921，main仍显示“尚无模型结果”。本次发布将结果提交作为祖先同步到main，避免继续依据旧状态安排工作。

## 下一步判断

先验证满容量时保护已验收记忆是否改善复用和回归表现。它有明确代码原因，可以只改一个策略；不先放宽guard，不同时改场景/训练预算/阈值。保留9不保证会匹配、通过验收或提高AP；新计划必须允许这一假设被否定。

唯一后续任务见[Protocol-028单任务指示](PROTOCOL028_SINGLE_TASK_DIRECTIVE_20260922.md)。当前只提出并登记计划，尚未实现或运行028。
