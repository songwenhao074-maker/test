# Protocol-029：一次休眠记忆效用诊断
日期：2026-09-22。状态：planned_not_implemented_not_run。基点：8dca4daa20f7d73fccff06c902204be51ebd9500；028已完成，本文件是当前唯一任务。

## 唯一任务
**在028原样在线回放中，只增加旁路预测，回答“有用记忆没有被选中，还是保留下来的记忆没有预测收益”。** 只运行一次D_memory_protected诊断回放；不新增训练方法，不重新训练C，不自动接着改算法或数据。实现检查和这次回放属于同一任务。

## 固定条件
继承028的种子700/model1、初始化、birth、满容量保护、路由、在线优化、reuse周期32/验证16、1%改善、全部normal/FPR/guard门槛、最多8驻留专家及延迟标签规则。旁路观测不得进入控制器决策，不训练任何额外参数，不占据或延长原生命周期phase。保留所有历史结果。

冻结输入仍为protocol027_data_revision_002：run35682811782/artifact10675401651，名称protocol027-frozen-data-35682811782，ZIP SHA256=111a4c5fc5c508a9e169fdbffe823ee36dc66c037c1775a6aa1abdbaba31c0a1。stream SHA256=fdea84306ac752611e4d0b1b4cd2300d0e0dcbc62ace07ac94096d904b310761。不重新模拟、不改027数据注册快照；029另建方法/诊断登记。

参照028完整证据：run35705211072/artifact10684617851，名称protocol028-single-pilot-35705211072，ZIP SHA256=e3be1c97d0f629c9ed40f61cfd4561b6b04e37b68357a2c92a23ac8f14e2e27e。用于核对原始预测、生命周期以及既有C指标。

## 旁路观测
- 每个预测时点，在真实预测产生后、对应标签公布前，复用该时点缓存的z与base logits，以当前四个通用专家分别搭配每一个已验收的休眠专家，生成“替换当前专用专家”的候选预测；使用现有replacement-consistent preview，不能把候选叠加为第六个常驻活动专家。记录候选ID、时间、标签尚不可用的证据、路由概率、正常/异常logits。
- 同时旁路记录“四个通用专家、不加任何专用专家”的预测，仅作归因参照。若候选仅胜过真实D却不胜过这一参照，不能直接称为旧知识收益。它不是新增训练组，不能当作正式C。
- 按原reuse每32个成熟区间的全部到期点建立审计记录，包括busy和no_match。冻结该点的休眠ID集合，并使用之后16个**新预测区间**的标签前记录；只在标签成熟后计算与真实live完全配对的loss、normal probability、FPR。不得用选择前已经见过标签的行代替未来验证。若专家不再休眠或流尾不足16区间，标记censored，不能强填成失败或通过。
- 在这16区间全部标签成熟的时刻，为每个候选计算原027正常guard，保留1%改善及全部原门槛。分别记录相似度是否够、是否最高、原控制器是否busy、若只看该旁路验证是否满足所有验收门槛。审计guard也不得修改模型缓存、优化器或随机数状态；业务ID/阶段表只在事后汇总使用。
- 核心汇总为：完整机会数、无记忆/截尾数、存在满足全部门槛候选的机会数、这些候选是否被相似度筛选或busy挡住；按专家及九个原回归窗口分解，列出各条件的交集，不把重叠原因计数直接相加。
- 补充各休眠专家在九个first100窗口中有覆盖部分的AP、FPR、相对同期live及四通用专家参照的差值；明确覆盖区间/行数，缺失写NA。只有覆盖相同完整窗口的数值才能直接对照已发布C窗口AP；不得拿短子集AP与C完整100步AP比较。报告全部九窗口，不能只选S3。

## 一致性与执行
新增独立029入口和输出目录，旧028实现及注册不改。通过无梯度、无状态变更的旁路实现；检查标签时序、替换拓扑及观测前后参数/优化器/随机数状态不变。实际D预测与028逐行比对（同环境优先精确相等，跨环境最大绝对概率误差≤1e-6），原生命周期离散事件/验收决定必须一致；主AP差≤1e-6。若不一致，先把本次标为诊断无效并排查旁路侵入，不能将偏移后的轨迹宣称为同一028复现。本任务不允许借修复改变科学设定。

采用028依赖版本与线程设置。执行模型实现.github/workflows/protocol029-diagnostic.yml，在专用分支protocol-029-memory-utility-20260922使用仅针对该workflow文件的push触发，保留workflow_dispatch可选；一次明确启动提交即可。不要借旧028启动入口运行新任务。普通文档/结果提交不触发模型。数据或原始证据缺失就记录具体阻塞，不临时换seed、数据或主指标。

## 判断、交付和停止
这是诊断，不设置“必须D胜出”的验收条件：
- 未选中候选确有同期收益且满足原门槛：支持筛选或调度存在漏检机会；仍不证明切换后的整段D必胜C。
- 收益主要被guard挡住：单独报告当前业务收益与正常背景代价，不能自行放宽guard。
- 现有记忆在这些候选/时点没有稳定收益：本轨迹下冻结记忆可复用性证据不足；不能直接归咎于删除或匹配。输出证据后停止，不自动实现再训练或重做场景。
- 标签事后选择的最好候选必须标注post_hoc_oracle_diagnostic；它不是实际在线策略，不纳入D>C正式结论。busy状态的可用候选也仅代表调度机会，不能假定切换没有后续代价。

交付docs/PROTOCOL029_RESULTS.md、紧凑diagnostic_summary/opportunity_audit/consistency/status JSON，以及包含全部标签前预测和完整日志的artifact链接/SHA。独立记录旁路新增耗时及内存；029总耗时包含诊断开销，不与旧C时间直接宣称方法成本变化。结果回写main并同步README、AGENTS、NEXT、PROJECT_CONTEXT和当前handoff为完成/停止状态。**报告一个主要发现后停止，不追加A/B、种子、调参或下一协议。**
