# Protocol-027数据身份修订：2026-09-22

用户授权修改导致实验停滞的不合理限制。本次修改范围是数据版本、数据交接和执行门禁；没有启动C/D，也没有修改方法、业务规律、时间线、模型种子或评价指标。后续仍只有一次C_fixed5/D_dynamic任务。

## 为什么可以改变

旧完整文件46b1…/fc3e…没有保全，精确恢复不稳定；这不能作为永久禁止研究继续的理由。当前候选fdea…/21cd…在多个模型运行前的恢复中出现，已经有现存artifact。此次将其明确登记为`protocol027_data_revision_002`，登记依据是数据可取得和可审计，不是模型成绩。旧登记保存为`registration_original_20260921.json`，旧失败和无法证实完整数组等价的事实保留。

这不是放宽SHA容差或接受任意新文件：新版本仍固定自己的stream、尾chunk、事件表SHA，并在冻结后逐文件校验。旧025的“一次修订/不得再修订”和027的旧哈希要求属于历史协议，本次用户授权的027独立版本修订明确替代其当前执行限制。

## 不再重跑最后一步

- 固定从run35604239834 / artifact10641512835读取完整stream、公共特征、manifest和尾chunk。
- 从run35292728416 / artifact10526683176读取27个前缀chunk、transient与保全manifest。固定manifest SHA，校验所使用文件，并将所有前5520行与候选逐数组比较；尾chunk也与候选完整stream比较。
- 此前云端artifact漏存events。仓库补入65,047字节的`events.json.gz`，原始表1,404,280字节。来源是2026-09-21同一保全state_5520的Windows恢复产物；其22个完整stream数组与云端候选完全一致。原始事件表SHA为`b87efc921ab23bed821871c573131df36db1c7a3b78e6a88eddd0a1374c94909`。这是一项公开记录的元数据补齐，不声称旧46b1…文件已恢复。

准备脚本只读取/复制文件，模拟步数为0，不读取大dill状态，不再依赖最后一步模拟的硬件数值行为。云端与本地使用同一套准备代码。

## 审计、冻结与模型入口

审计器与运行器共用新版本登记，消除旧SHA硬编码分叉。完整027资格审计保持真实物理标签、公共特征因果一致性、正常guard及九窗口覆盖检查；源025两类guard的原失败不被改写。

冻结清单包括38个文件及它们的SHA/大小：所有模型输入、events、全部28个chunk、manifest、源审计、027资格、组装核验及注册快照。导出时不包含2.3GB恢复状态。归档成功后记录artifact ID/digest和冻结清单SHA，才允许启动父进程；两组子进程再独立核验同一冻结数据。缺events、输入被改动、数据锁版本不符、归档失败均不能训练。

后续通过`frozen_data_run_id`下载完整冻结artifact，复核后复用，无需重复组装或模拟。再上传完整副本延续保留期；部分诊断artifact不作为可训练数据包。工作流仅手动触发，不因本次上传而自动启动实验。

## 本次实际验证

本地复制既存文件，前5520行与保全chunk/transient完全一致；完整027资格通过，960个正常guard行、九回归窗口有效，5520步特征最大误差3.9411090790864023e-07（原容差5e-5）。完整冻结、导出及重新读取审计使用同一实际数据进行验证。

26项针对性测试通过，覆盖缺失事件、修改冻结特征、完整包不依赖checkpoint即可复用、哈希错配拒绝、未归档不能宣称ready，以及既有特征/guard/替换行为。证据保存在`artifacts/ftmoe_online/protocol_027/maintenance_20260922/`。

本次验证不是模型实验，也未生成云端完整冻结artifact。接手模型从main手动执行当前单任务；工作流先把完整数据真正归档，再运行C/D并报告结果后停止。
