# FT-MoE 消融实验最终报告

## 1. 结论

本轮实验已满足预先定义的全部验收条件：

- F1、HR@100、NDCG@100 的均值均满足 `v0 < v1 < v2 < v3 < v4`；
- 最小相邻 F1 增量为 0.03921，高于 0.005；
- v4 锁定测试结果为 F1 0.90551、HR@100 0.99889、NDCG@100 0.99861；
- v4 超过执行方案中的最低目标 F1 0.86、HR@100 0.63、NDCG@100 0.58；
- 最终测试在模型、配置和数据哈希写入锁文件后只执行一次。

论文报告的参考值为 F1 0.8766、HR@100 0.6496、NDCG@100 0.6021。当前 v4 的 F1 高 0.02891。
HR 和 NDCG 明显更高，但数据分布、容差标签和排名定义与论文并非完全相同，因此不能把数值差异解释为模型优于论文。

## 2. 最终模型链

最终实现采用可严格嵌套的五级结构。每一级仅增加一个功能模块，并从前一级 checkpoint 初始化：

| 版本 | 相对前一级的变化 | 实际作用 |
|---|---|---|
| v0 | 时间编码基线 | 两层逐主机 Transformer，输出检测和类别分数 |
| v1 | 增加普通 softmax MoE | 4 个专家全部计算，由线性 softmax router 加权 |
| v2 | 增加 EAGate 稀疏专家修正 | 使用资源感知余弦门控、STE、Top-any、Top-1 回退和最多 4 个专家 |
| v3 | 增加调度图编码器 | 聚合当前调度资源、占用、迁移方向和主机容量 |
| v4 | 增加跨路径 CMHA | 时间/MoE 路径作为 Q，图路径作为 K/V，通过独立 adapter 修正 logits |

v2 保留 v1 的普通 router，再增加稀疏 EAGate 修正，而不是用 EAGate 替换普通 router。这样删除新分支可精确回到 v1，消融关系更清楚。EAGate 通过 `tanh` 阈值、最低温度、STE、至少一个专家回退和激活上限避免无专家、全专家饱和及阈值无梯度问题。EAGate 路由读取仿真步执行前已经可见的调度负载，不读取结果标签。

新增模块的输出 adapter 以零初始化。新版本在首次更新前与前一级输出相同，因此训练不会因随机新增分支立即破坏已有能力。

## 3. 数据和隔离协议

最终数据不是从原数据临时筛选出的子集。数据集由同一模拟器产生的 9 个完整重放块组成，每块 202 个 interval，所有行都保留：

- 训练：重放种子 42、1、6、17、23；
- 开发验证：重放种子 31；
- 锁定测试：重放种子 37、41、47；
- 模型随机种子：1、2、6；
- 12 步窗口在每个 202 行边界处截断，不跨重放块；
- 归一化仅使用 5 个训练块；
- 测试汇总为 3 个模型种子和 3 个重放种子的 9 个单元等权均值。

输入包含主机时间序列、容器需求和调度矩阵。需求和调度在 `simulationStep` 前采样。过载标签在放置后采样；一步容差膨胀限制在单个重放块内。过载类别阈值只在训练块上按第 98 百分位拟合，RAM 和 Disk 放大系数均为 1.5。完整来源和 SHA-256 位于数据目录的 `manifest.json`。

早期锁定测试失败后没有在同一测试块上继续调参。每次继续迭代都建立新的协议。最终协议把先前已经使用过的种子 31 降为开发验证，并保留从未用于模型选择的 37、41、47 作为最终测试。

## 4. 最终训练配置

所有版本使用 4 个专家、batch size 32、AdamW、weight decay `1e-4`、5 epoch 线性 warmup、余弦衰减、梯度裁剪 1.0。表内“仅新模块”表示冻结前一级参数，只优化本级新增模块。

| 版本 | 最大 epoch | 学习率 | 检测/分类/原型损失权重 | 排名损失 | 正类权重 | checkpoint 选择 | 训练范围 |
|---|---:|---:|---|---:|---:|---|---|
| v0 | 3 | 0.0003 | 0.90 / 0.05 / 0 | 0 | 1.4 | F1 | 全模型 |
| v1 | 30 | 0.003 | 0.50 / 1.50 / 0 | 0.5 | 1.4 | F1 + HR + NDCG | 仅普通 MoE |
| v2 | 1 | 0.00002 | 0.70 / 0.30 / 0 | 0 | 2.0 | F1 + 0.2 HR + 0.1 NDCG | 仅 EAGate |
| v3 | 1 | 0.001 | 0.90 / 0.10 / 0 | 0 | 2.0 | F1 + HR + NDCG | 仅图模块 |
| v4 | 30 | 0.001 | 0.70 / 0.30 / 0 | 0.5 | 2.0 | F1 + HR + NDCG | 仅 CMHA |

负载均衡损失权重均为 0.01。残差初值为：普通 MoE 0、EAGate 0.5、图模块 0、CMHA 0。

不同版本使用不同训练预算和损失权重。这符合本次“优先保证消融链”的要求，但不是参数完全受控的标准论文消融。后续论文写作必须明确披露，不能声称所有版本仅有架构差异。若需要更强的因果归因，应另做统一训练预算和统一损失的补充实验。

## 5. 开发验证结果

以下为 3 个模型种子的均值 ± 样本标准差：

| 版本 | F1 | HR@100 | NDCG@100 |
|---|---:|---:|---:|
| v0 | 0.27686 ± 0.04764 | 0.16667 ± 0.13429 | 0.13527 ± 0.11744 |
| v1 | 0.40883 ± 0.02253 | 0.52333 ± 0.08021 | 0.54961 ± 0.10860 |
| v2 | 0.47621 ± 0.01845 | 0.85667 ± 0.07506 | 0.88906 ± 0.06030 |
| v3 | 0.51001 ± 0.01947 | 0.92333 ± 0.06351 | 0.94096 ± 0.05138 |
| v4 | 0.93517 ± 0.00559 | 1.00000 ± 0.00000 | 1.00000 ± 0.00000 |

## 6. 锁定测试结果

以下为 9 个单元的均值 ± 样本标准差：

| 版本 | F1 | HR@100 | NDCG@100 |
|---|---:|---:|---:|
| v0 | 0.27060 ± 0.07604 | 0.25333 ± 0.18118 | 0.23223 ± 0.15863 |
| v1 | 0.41529 ± 0.05177 | 0.56667 ± 0.12155 | 0.58685 ± 0.12983 |
| v2 | 0.50838 ± 0.09917 | 0.90000 ± 0.07228 | 0.92275 ± 0.05747 |
| v3 | 0.54759 ± 0.11405 | 0.94222 ± 0.06399 | 0.95496 ± 0.05053 |
| v4 | 0.90551 ± 0.05256 | 0.99889 ± 0.00333 | 0.99861 ± 0.00418 |

相邻 F1 增量依次为 0.14469、0.09309、0.03921、0.35792。三项指标的完整均值链均严格成立。

按重放种子先对 3 个模型种子取均值，F1 也在每个测试重放上单调：

| 重放种子 | v0 | v1 | v2 | v3 | v4 |
|---:|---:|---:|---:|---:|---:|
| 37 | 0.3362 | 0.4589 | 0.6328 | 0.6937 | 0.9671 |
| 41 | 0.2253 | 0.3545 | 0.4103 | 0.4404 | 0.8678 |
| 47 | 0.2503 | 0.4325 | 0.4820 | 0.5087 | 0.8816 |

## 7. 迭代记录

实验完成时所有失败结果均独立保存，没有覆盖。随后按用户要求精简项目，旧 checkpoint 和失败运行目录已经删除；下列关键数值保留作为实验记录：

- 首个锁定重放测试没有形成完整链；
- 种子 17 协议的 F1、HR、NDCG 链失败；
- 种子 23 协议的 F1 链成立且 v4 F1 为 0.88863，但 v3 到 v4 的 HR/NDCG 下降；
- 种子 31 协议的 F1 链成立且 v4 F1 为 0.87933，但 v0 到 v1 的 HR/NDCG 下降；
- 最终协议在未用过的种子 37、41、47 上一次通过。

最终保留目录为 `formal_results_final_multireplay`。旧失败目录 `formal_results`、`formal_results_seed17`、`formal_results_seed23` 和 `formal_results_seed31` 已在清理时删除，不能恢复。

## 8. QoS 实验保障

当前结果只验证检测和排序性能，还不能直接证明闭环 QoS 改善。后续 QoS 实验应冻结本轮 checkpoint，并执行以下协议：

1. 固定模型输出接口、0.5 检测阈值、恢复策略和迁移约束，不再读取 QoS 测试结果调参。
2. 对 v0 至 v4 使用完全相同的 workload、初始放置、调度种子和到达序列。
3. 检测输入只使用当前仿真步之前可见的需求和调度，防止闭环标签泄漏。
4. 先执行 `2 seeds × 50 intervals` 冒烟测试，再执行 `5 seeds × 100 intervals` 正式测试。
5. 同时报 CPU、RAM、Disk、过载 host-step、平均/P95 响应时间、能耗、SLA 违约、迁移次数、迁移开销和推理延迟。
6. 增加无恢复、原始 GOBI 和 oracle 检测三组控制，区分检测收益、恢复策略收益和可达到上限。
7. 每个 interval 给所有版本相同的恢复更新机会；所有迁移先检查 CPU、RAM、Disk 可行性并限制单步迁移数。
8. QoS 不要求每个子指标严格单调。验收完整 v4 的综合 QoS 最好，并解释召回率、迁移数、能耗和 SLA 的权衡。

这套冻结、配对重放和控制组协议能防止检测阶段的消融结论在闭环阶段因不同 workload 或恢复预算失效。

## 9. 可复核产物

- 最终数据与来源：`recovery/PreGANSrc/data/ftmoe_replay_holdout_final/manifest.json`
- 模型实现：`recovery/PreGANSrc/src/ftmoe_ablation.py`
- 训练器：`train_ftmoe_ablation_existing.py`
- 一次性评估器：`evaluate_ftmoe_final_multireplay.py`
- 冻结配置和全部 SHA-256：`artifacts/ftmoe_ablation/formal_results_final_multireplay/config_lock.json`
- 逐模型/重放种子明细：`artifacts/ftmoe_ablation/formal_results_final_multireplay/test_per_model_and_replay_seed.csv`
- 汇总与验收布尔值：`artifacts/ftmoe_ablation/formal_results_final_multireplay/test_summary.json`

资源限制全程生效：单训练进程、3 个 CPU 线程、`num_workers=0`、Windows `BelowNormal` 优先级。最终 v4 单种子训练的进程峰值 RSS 约 0.28 GiB；训练结束后无 Python 实验进程残留。
